# raw CSV (too large for git — use the parquet in /data instead)
data/*.csv

# secrets — never commit
.env
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# notebooks
.ipynb_checkpoints/

# OS
.DS_Store
